<section>
  <article>
  {{#each posts}}
    <h2><a href="{{post_path this}}">{{this.title}}</a></h2>
    {{this.date}} || <span>{{this.tags}}</span>
    <div>
      {{{md this.md}}}
    </div>
    <a href="{{post_path this}}">Read More ...</a>
  {{/each}}
  </article>
  <aside>
    <h3>Projects</h3>
    <a href="http://openstack.astokes.org">Ubuntu OpenStack Installer</a>
  </aside>
</section>
