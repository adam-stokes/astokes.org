<section>
  <article>
  {{#each posts}}
    <h2 class="article-header"><a href="{{post_path this}}">{{this.title}}</a></h2>
    <span class="article-date-pad-left">Posted on {{pretty this.date}}</span>
    <div>
      {{{md this.md}}}
    </div>
  {{/each}}
  </article>
  <aside>
    <h3>Projects</h3>
    <a href="http://openstack.astokes.org">Ubuntu OpenStack Installer</a>
  </aside>
</section>
